namespace STS2Mobile.Launcher;

internal static partial class LauncherRuntimeCacheEvidence
{
    private const string UtcMillisPrefix = "UTC millis:";
    private const string PackagePrefix = "Package:";
    private const string VersionNamePrefix = "Version name:";
    private const string VersionCodePrefix = "Version code:";
    private const string AssemblyCacheSchemaPrefix = "Assembly cache schema:";
    private const string SelectedBranchPrefix = "Selected branch:";
    private const string SelectedBranchRequiresRuntimePackPrefix = "Selected branch requires runtime pack:";
    private const string RuntimeIdPrefix = "Runtime ID:";
    private const string RuntimeSourcePrefix = "Runtime source:";
    private const string RuntimePackDirectoryPrefix = "Runtime pack directory:";
    private const string RuntimePackGameAssemblyPrefix = "Runtime pack game assembly:";
    private const string GameDirectoryPrefix = "Game directory:";
    private const string SelectedPckPathPrefix = "Selected PCK path:";
    private const string SelectedPckIdentityPrefix = "Selected PCK identity:";
    private const string SelectedPckSha256Prefix = "Selected PCK SHA256:";
    private const string SelectedSourceAssemblyPrefix = "Selected source sts2.dll:";
    private const string SelectedSourceAssemblySha256Prefix = "Selected source sts2.dll SHA256:";
    private const string ActiveSourceAssemblyPrefix = "Active source sts2.dll:";
    private const string ActiveSourceAssemblySha256Prefix = "Active source sts2.dll SHA256:";
    private const string PublishCacheDirectoryPrefix = "Publish cache directory:";
    private const string PublishCacheActiveAssemblySha256Prefix = "Publish cache active sts2.dll SHA256:";
}
