namespace STS2Mobile.Launcher;

internal static partial class LauncherPortalUxSupport
{
    internal const string Model =
        "Status-led launcher portal with titled Steam sign-in, Steam Guard, game install, play/sync, and diagnostics sections.";

    internal const string CompactReadyStatePriorityDescription =
        "compact ready-state priority that keeps the ready summary, Save Check shortcut, and Get-saves-first cloud controls before Start Game while moving version management below the primary launch path";

    internal const string CompactReadyStateCloudOptionsBelowLaunchDescription =
        "compact ready-state cloud options stay below Start Game as an optional save-settings drawer after Get-saves-first cloud controls";

    internal const string CompactPlainLanguagePlaySyncLabelsDescription =
        "compact Play/Sync uses plain-language save copy such as Get Steam Saves, Upload Locked, Save Check, and Fixes & Help while keeping upload collapsed and explicit";

    internal const string CompactSupportToolLabelsDescription =
        "compact user-facing support tool labels such as Safe Start / Cloud off, Check Files / Updates, Game Versions / Refresh list, Repair Files / Rebuild game, Free Space / Old versions, Help Report / Share details, Last Problem / Open details, and Copy Log / Review first";

    internal const string CurrentImplementation =
        "The launcher shell uses a branded atmospheric backdrop with explicit RGBA drawing colors, stronger rounded high-contrast actions, a branded header with plain-language mobile subtitle copy, readable phase-labeled status capsule with a structured phase chip and error-first guided next-action label, compact plain-language status copy for sign-in, ownership check, install-needed, and ready-to-play states, compact vertical next-step hero, compact low-profile status card with a responsive inline phase/next-action headline where width allows, stacked phase and next-action labels on narrow compact screens, viewport-aware compact status headline reflow after Android rotation or keyboard viewport changes, plus a stable one-line non-attention detail row with short mobile status copy, a visible Details/Hide cue, and a touch-safe button for expanding full details, compact touch-safe responsive numbered workflow step strip with visible two-line step labels, separate readable number badges, and a unified 62px touch height that stays one dense row even on narrow compact viewports, grouped with a subdued inline current-task jump button and two-line workflow cells that share the 62px compact touch height in a low-profile toolbar shell around the touch-safe sticky compact task header that reflows between inline and stacked layouts after Android rotation or keyboard viewport changes, tappable compact workflow steps that directly navigate to visible or fallback task sections, compact current-task jump button anchored as a touch-safe sticky compact task bar with short structured two-line app-like task title/detail labels and contextual task detail labels, compact two-line control detail labels use a readable shared compact detail font, compact touch-safe confirmation dialogs with wider scroll-safe warning text, contextual confirm/cancel labels, and current-viewport sizing after rotation or keyboard viewport changes, native Android credential buttons explicitly disable platform all-caps text transformation so Next, Show password, Cancel, and Sign in with Steam remain sentence-case, compact Android sign-in primary action before helper copy, compact Android sign-in CTA promoted to a large primary action with Sign in with Steam / Android login detail labels before the readable two-line password-manager safety helper, larger compact Steam Guard code entry and Verify Code controls before helper copy, inline where width allows and stacked full-width on narrow compact viewports, viewport-aware compact Steam Guard code/action row reflow after Android rotation or keyboard viewport changes, compact Steam Guard submit detail labels that keep Verify Code / Submit once action-first, compact Steam Guard bounded two-line helper labels that keep one-shot/no-storage and newest-code retry guidance below the action row, compact failure recovery promoted to a primary Try Again / Restart task action while cloud controls remain hidden, compact completed-auth section suppression, task-led primary action wording, consistent Start Game primary CTA with compact Start Game / Ready version detail labels, touch-first action targets, rounded shared metric scaling so compact Android fonts, touch targets, separators, and margins do not lose fractional scale, Android compact touch-scale floor for small-device readability, Android-readable shader warmup/loading screen with mobile-width compact panel and taller styled percentage progress, Android-readable startup status card with framed mobile-width status copy after the launcher closes, touch-safe compact version dropdowns with larger opened popup row spacing/padding, quick-start guidance with row-based compact phone steps and a visible Upload locked row, bounded compact quick-start guide panel, collapsible compact quick-start guidance with structured Quick Start / Get saves first title/detail labels and bounded guide row cards, compact active-task safe-flow suppression after setup, compact ready-state safe-flow suppression, compact ready-state install-section suppression, compact install step ordering with selected-version summary and a large Download Version / Local files only primary action before optional version details, compact install primary action detail labels for download, redownload, retry, and disabled downloading states, compact install-version drawer controls with structured title/detail labels that keep the version dropdown and refresh action inline where width allows and stack on narrow compact viewports, compact download progress promoted directly under the disabled Downloading primary action with a stable two-line Downloading selected version status label and taller styled percentage bar, collapsible compact version details with structured version-file drawer labels and bounded two-line Files for / Play version helper labels, compact readable selected-version and ready-version summary cards, compact responsive selected-version summary that stays explicit about Cloud unchanged/local-file scope and opens the version drawer from a touch-safe Change shortcut, compact ready-version summary, compact ready-version summary panel, compact responsive ready-version summary with Save Check shortcut opens Save Check from a touch-safe Upload-locked cue without unlocking Push, compact Play/Sync drawer detail labels rendered as structured title/detail action labels, compact responsive action rows that keep save get/upload actions, save settings, and support tools side-by-side where space allows and stack them into full-width controls on narrow compact viewports, compact support tool detail labels including Safe Start / Cloud off and Copy Log / Review first for launcher-log safety, compact Save Backup / Local safety and Cloud Sync / Steam saves option detail labels, collapsible compact Save Check / Get saves first cloud-safety drawer labels shown before get/upload actions with a stable two-line Saves for / Get Steam saves before upload / Upload can overwrite Steam detail label, collapsible compact cloud options, compact cloud action labels that name Android as the Pull destination and Steam as the Push destination, compact Pull detail labels that keep Get Steam Saves / Download to Android direction-explicit, compact locked Push toggle labels that stay structured as Upload Locked / Review first and Hide Upload / Keep locked while Push controls remain collapsed until explicitly opened, compact dangerous Push detail labels that keep Upload to Steam / Overwrite cloud and Confirm Upload / Overwrite cloud direction-explicit after unlock, compact armed Push overwrite warning rendered as a stable Steam Cloud overwrite / Confirm only after Pull/local saves are verified label, compact drawer state reset when drawers leave scope, compact touch-safe dense drawer toggles before expanded drawer details, compact drawer collapse after version selection, compact state-transition active-section scroll, compact primary-action scroll anchors, padded compact scroll anchors that keep jumped-to task sections below the sticky header instead of flush against the viewport edge, viewport-aware compact task re-anchoring after rotation or keyboard viewport changes, compact bottom scroll breathing room, compact low-profile attribution footer, viewport-aware keyboard offset refresh plus keyboard-focused input scrolling so managed Steam Guard and fallback credential fields stay reachable above the soft keyboard, primary cloud actions before lower-frequency cloud options, safer Pull-before-Push cloud action ordering, armed Push overwrite warning, compact button labels, scroll-safe startup recovery controls, structured compact startup recovery actions such as Restart App / Open launcher, Safe Start / Cloud off, Help Report / Share details, Copy Log / Review first, and Hide Help / Keep waiting, reduced compact header chrome with a condensed single-line compact brand header and readable compact brand subtitle, compact dense vertical rhythm with single-row section headers and explicit short task cues such as Steam account, Current code, Local files, and Play safely, compact scroll-hosted Help & Reports drawer with structured touch-safe drawer title/detail labels, plain-language help-report and launcher-log status copy, a readable bounded compact diagnostics log viewport, and viewport-aware diagnostics log resizing after Android rotation or keyboard viewport changes, compact section-header subtitle suppression, dynamic compact content width, tablet/wide-screen responsive content width, top-anchored portal content, mobile-first compact panel sizing with dense compact shell padding, explicit titled state sections, hidden diagnostics drawer with automatic opening for explicit diagnostics actions, styled native fallback recovery actions with verbose native fallback diagnostics collapsed until requested and responsive recovery rows on narrow landscape screens, suppressed raw startup fallback banner, ready-state version-install/cloud-save separation guidance, and branch/cloud helper text while preserving existing SteamKit, download, cloud, and launch flows.";

    internal const string ValidationBoundary =
        "Portal layout, scaling, readability, warmup/startup screen readability, native login panel placement, hidden diagnostics behavior, and next-action clarity still require ARM64 visual validation on real devices.";
}
