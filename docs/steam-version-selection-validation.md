# Steam Version Selection Validation

Evidence capture template: `docs/steam-version-selection-evidence-template.md`
Completion audit tracker: `docs/steam-version-selection-completion-audit.md`
Architecture note: `docs/steam-version-selection-architecture.md`
Save compatibility matrix: `docs/steam-version-selection-save-compatibility.md`
User/tester guide: `docs/steam-version-selection-user-guide.md`
Release note snippet: `docs/steam-version-selection-release-note-snippet.md`
Validation runbook: `docs/steam-version-selection-runbook.md`
Release readiness tracker: `docs/steam-version-selection-release-readiness.md`
Static audit helper: `scripts/audit-steam-version-selection.ps1`
Managed/native guidance parity helper: `scripts/audit-steam-branch-guidance-parity.ps1`
Evidence folder scaffold helper: `scripts/new-steam-version-selection-evidence.ps1`
Device evidence capture helper: `scripts/capture-steam-version-selection-evidence.ps1`
Beta integrity evidence helper: `scripts/capture-steam-beta-integrity-evidence.ps1`
Tooling command reference: `docs/steam-version-selection-tooling.md`
Tester issue template: `.github/ISSUE_TEMPLATE/steam_version_selection_report.md`

Current release decision: not release-ready. Static implementation/docs/CI guardrails are in place, but ARM64 evidence is still missing for public/default regression, beta branch download/startup routing, public-vs-beta depot integrity, cache cleanup, inaccessible/private/password branch behavior, Pull-after-switch/current-backup safety, password-manager suggestion behavior in the native credential panel, artifact hygiene review, and save compatibility. The release-readiness tracker is the source of truth for the current signoff contract. Android Steam login proof is tracked in [Android Steam login validation](android-steam-login-validation.md).

Current implementation status:

- The launcher persists the selected Steam branch in `game_branch`.
- The launcher exposes game version selection as a discovery-led dropdown. `public` remains available by default, account-visible branches are added from Steam app-info after refresh, and previously saved custom branch values are preserved as selectable options for compatibility/retry diagnostics without reintroducing manual text entry.
- During download/update app-info resolution, the downloader logs account-visible Steam branch availability, Windows depot manifest counts per visible branch, and selected-branch visibility/downloadability evidence for failed non-public downloads. It also writes `last_steam_branch_availability.txt` so diagnostics can report the latest branch availability evidence after the live log has scrolled away. Failed download/update UI status should compact that marker into a user-facing branch availability diagnosis without requiring adb/logcat.
- The default/public branch uses the legacy `files/game` and `files/download_state` paths.
- Non-public branches use side-by-side caches under `files/game_versions/<safe-branch>/game` and `files/game_versions/<safe-branch>/download_state`, with diagnostics naming the active install slot kind and slot directory.
- Download, update check, redownload, native startup, managed startup fallback, diagnostics, and inactive-cache cleanup are branch-aware.
- Completed downloads write `steam_branch.txt` into the selected game directory so branch ownership, install slot, selected/public depot manifest comparison, and explicit public-inherited depot content can be audited from the installed cache.
- Branch switching shows a warning and enables local backups before applying the selected branch.

Release-readiness blockers:

Build gate: validate the managed C# launcher code and Android Java entry points compile after branch marker readiness, diagnostics, and Push guard changes before treating any runtime validation as meaningful.

1. Validate public/default branch still downloads, updates, and launches from the legacy `files/game` path after an upgrade from a previous public APK.
2. Validate selecting an account-visible non-public branch persists `game_branch=<selected-branch>` and downloads into `files/game_versions/<selected-branch>/game`.
3. Validate Android native startup loads the selected non-public PCK from `files/game_versions/<selected-branch>/game/SlayTheSpire2.pck`.
4. Validate switching back to default/public uses `files/game/SlayTheSpire2.pck` without deleting the non-public cache.
5. Validate completed downloads write `steam_branch.txt` in the selected game directory with the selected branch, state directory name, install slot kind, install slot directory, depot manifest count, depot manifest IDs, selected branch manifest IDs, public manifest IDs, manifest source, manifest request branch, public-match counts, inherited-public counts, and missing-selected-manifest counts. Validate non-public caches are not treated as ready if the marker is missing, names a different branch, lacks install-slot provenance, or lacks depot manifest provenance; upgraded default/public installs may remain ready without a marker.
6. Validate `REDOWNLOAD SELECTED VERSION` removes only the selected branch cache and preserves inactive branch caches.
7. Validate `CLEAR CACHED VERSIONS` removes inactive non-public caches and stale runtime packs while preserving the selected branch cache and selected runtime pack when present. The launcher status/log must name the selected version preserved by cleanup and report both removed game-version cache count and removed runtime-pack cache count. Logcat must include removed inactive cache paths, removed runtime pack paths, and the preserved selected cache/runtime-pack path when a selected non-public cache/runtime pack exists. Diagnostics must show current selected branch context plus the game version cache cleanup marker filename/path/presence/UTC/UTC parseability/selected branch/selected-branch match/selected version/selected version slot kind/selected version slot directory/game_versions presence/runtime_packs presence/selected runtime pack directory/selected runtime pack pre-cleanup presence/removed count/removed runtime pack count/selected-cache-preserved aggregate/selected-runtime-pack-preserved aggregate. `last_game_version_cache_cleanup.txt` must record parseable UTC, selected branch, selected version, selected version slot kind, selected version slot directory, runtime pack directory state, selected runtime pack state, game_versions presence, removed count, removed runtime pack count, removed caches, removed runtime packs, and preserved selected cache/runtime pack where applicable.
8. Validate diagnostics report selected branch, selected game version display name, selected branch storage directory, selected game version slot kind, selected game version slot directory, selected game directory, selected game PCK path, final selected game files readiness, selected readiness problem, selected download state, selected game branch marker path/presence/branch/install-slot-kind/install-slot-directory/expected-install-slot-kind/expected-install-slot-directory/matching install-slot provenance/depot-manifest provenance/branch-integrity provenance/count/readiness, and cached non-public version directories with `selected=true/false`, `inactive=true/false`, `branchMarkerPresent=true/false`, `branchMarkerBranch=<value>`, `branchMarkerExpectedInstallSlotKind=<value>`, `branchMarkerExpectedInstallSlotDirectory=<path>`, `branchMarkerMatchingInstallSlotProvenance=true/false`, `branchMarkerDepotManifests=<count>`, `branchMarkerIntegrityProvenance=true/false`, `branchMarkerDepotsMatchingPublic=<count>`, `branchMarkerDepotsDifferingFromPublic=<count>`, `branchMarkerDepotsInheritedFromPublic=<count>`, `branchMarkerDepotsMissingSelectedManifest=<count>`, and `branchMarkerReady=true/false` inventory flags. Cached non-public `branchMarkerReady` must require matching branch directory, matching install-slot provenance, depot manifest provenance, and branch-integrity provenance.
9. Validate public-vs-beta depot integrity. After a clean selected beta redownload, capture the branch marker and public-vs-beta inventories. The marker and launcher diagnostics must show whether each depot is `manifestSource=selected` or `manifestSource=public-inherited`, the `manifestRequestBranch` used for Steam authorization, and whether the selected/effective manifest matches public. Download completion should log a selected-branch integrity summary naming public-inherited, public-identical, branch-specific, or missing selected-branch manifest evidence when present. If art assets look public while other behavior looks beta, capture the public inventory, beta inventory, `SlayTheSpire2.pck` hash comparison, affected art asset path/hash comparison, and the marker counts for public-identical, branch-specific, inherited-public, and missing-selected-manifest depots. The beta-integrity summary must include `Classification:`, `Evidence readiness:`, and `Evidence missing/weak:`. Treat `Evidence readiness: not ready for final classification` as a release blocker until the missing proof is captured or explicitly carried as an unresolved blocker.
10. Validate native startup logs include `Selected Steam branch`, `Resolved startup game directory`, Steam branch marker readiness, install-slot provenance, depot-manifest provenance, branch-integrity provenance, and depot manifest entry count, native fallback diagnostics include Steam branch marker readiness, install-slot provenance, depot-manifest provenance, branch-integrity provenance, and depot manifest entry count, and native startup does not launch a non-public selected PCK when `steam_branch.txt` is missing, names a different branch, lacks install-slot provenance, lacks depot manifest provenance, or lacks branch-integrity provenance.
11. Validate launcher status, dropdown labels, and buttons use selected-version wording while downloading, updating, redownloading, and launching. Ready and download-required status text must show the active install slot. Refreshed Steam app-info dropdown labels should expose concise metadata badges such as `(ready)`, `(build <id>)`, `(password)`, or `(unavailable)`, while helper text keeps the full selected-branch evidence. Download completion, post-download readiness failure, update-available, up-to-date, download failure, and update-check failure states should name the selected game version or show the selected-version readiness problem.
12. Validate selecting a cached non-public version with missing or mismatched `steam_branch.txt` shows a redownload/rebuild-cache message and `REDOWNLOAD SELECTED VERSION` action instead of treating it as a normal ready install. Confirming that action must delete the selected cache, write `last_game_version_redownload.txt` with parseable UTC, selected branch, selected version, selected version slot kind, selected version slot directory, deleted game directory, pre/post-delete game directory existence, deleted download state directory, and pre/post-delete download state existence, expose current selected branch context, redownload UTC parseability, selected-branch match, selected slot fields, and selected directories cleared status in diagnostics, log/status that the metadata cache was cleared, and then start the replacement download.
    If refreshed app-info evidence already blocks that selected branch, confirming the cleanup should delete only the selected cache and keep the replacement download blocked with selected-branch-specific status/log text.
13. Validate behavior when a selected dropdown branch does not exist, is stale from an older saved custom value, or is not visible to the Steam account. The selected-option helper text should say that a saved branch was not listed in the latest Steam app-info catalog and may be stale, private, inaccessible, password-protected, or unavailable. If refreshed app-info evidence already proves the selected non-public branch is password-protected, exposes no Windows depot manifest, or is an absent saved branch, the launcher should block selected-version download/update checks before contacting Steam and show selected-branch-specific `Download blocked` or `Update check blocked` status while still allowing APK/app update checks to complete. Failure evidence must include the launcher status branch availability diagnosis only when the availability marker matches the current active branch, the logged `Visible Steam branches:` summary, `last_steam_branch_availability.txt`, selected-branch visibility, selected-branch match against the current active branch, Windows depot manifest count for the selected branch, derived selected-branch downloadable/problem diagnostics, and whether any visible branch appears downloadable. Branch availability marker values must be single-line/sanitized; if Steam returns more branches than the marker captures, an overflow count must be recorded.
14. Validate behavior when a selected dropdown branch requires a Steam beta password. Password-protected branch behavior is not release-ready until this is proven or explicitly unsupported in the UI and diagnostics show `Steam beta password entry supported: false`.
15. Validate the beta switch confirmation warns that non-public branches may be private or password-protected, beta password entry is not implemented, local backup will be enabled, and Steam Cloud Push will require backup storage permission after switching.
16. Validate save compatibility before and after switching branches. Manual Push must require Pull-from-Cloud evidence for the currently selected version and Android local save evidence before upload, even without a branch switch. The Pull marker and diagnostics must record `Manual Pull completed before Push: true`, diagnostics must expose current important Android local save evidence count/presence, and diagnostics must expose `Baseline manual Push prerequisites satisfied`. Branch-switch evidence must also prove Pull-after-switch freshness for the selected version. Steam Cloud Push must remain treated as destructive across branch switches, and the Push confirmation must name the selected game version, selected version slot, Pull-after-switch requirement for that selected version, Android local save evidence, backup storage permission, local pre-Push backup evidence, and cloud pre-Push backup evidence.
17. Validate local backup is enabled before branch switching, manual Push is blocked after a branch switch until branch-switch marker safety evidence is complete for the currently selected branch, Pull from Cloud completes after that branch switch for the selected version, Android local save evidence exists, and storage permission is available. Diagnostics must show `Branch switch marker path`, `Branch switch marker present`, `Branch switch marker UTC`, `Branch switch marker UTC parseable`, `Branch switch previous branch`, `Branch switch selected branch`, `Branch switch selected branch selection kind`, `Branch switch selector mode`, `Branch switch selected version`, `Branch switch selected version slot kind`, `Branch switch selected version slot directory`, `Branch switch selected branch matches current selected branch`, `Branch switch selected branch note`, `Branch switch local backup forced`, `Branch switch manual Push requires backup storage`, `Branch switch warning acknowledged`, `Branch switch non-public warning acknowledged`, `Branch switch marker has required safety evidence`, `Branch switch marker has required safety evidence for selected branch`, `Push requires backup storage after branch switch`, `Manual Pull evidence marker filename`, `Manual Pull evidence marker path`, `Manual Pull evidence marker present`, `Manual Pull evidence UTC`, `Manual Pull evidence UTC parseable`, `Manual Pull evidence selected branch`, `Manual Pull evidence selected branch selection kind`, `Manual Pull evidence selector mode`, `Manual Pull evidence selected version`, `Manual Pull evidence selected version slot kind`, `Manual Pull evidence selected version slot directory`, `Manual Pull completion flag recorded`, `Manual Pull evidence is after branch switch`, `Manual Pull evidence matches selected branch`, `Manual Pull completed after branch switch for selected version`, `Manual Push evidence marker filename`, `Manual Push evidence marker path`, `Manual Push evidence marker present`, `Latest manual Push evidence outcome`, `Latest manual Push evidence UTC`, `Latest manual Push evidence selected branch`, `Latest manual Push evidence selected branch selection kind`, `Latest manual Push evidence selector mode`, `Latest manual Push evidence selected version`, `Latest manual Push evidence selected version slot kind`, `Latest manual Push evidence selected version slot directory`, `Latest manual Push evidence reason`, `Manual Push evidence UTC`, `Manual Push evidence UTC parseable`, `Manual Push evidence selected branch`, `Manual Push evidence selected branch selection kind`, `Manual Push evidence selector mode`, `Manual Push evidence selected version`, `Manual Push evidence selected version slot kind`, `Manual Push evidence selected version slot directory`, `Manual Push evidence recorded local backup count`, `Manual Push evidence recorded cloud backup count`, `Manual Push evidence recorded latest local backup UTC`, `Manual Push evidence recorded latest cloud backup UTC`, `Manual Push completion flag recorded`, `Manual Push evidence is after branch switch`, `Manual Push evidence matches selected branch`, `Manual Push evidence recorded pre-Push backup evidence satisfied`, `Manual Push completed after branch switch for selected version with backup evidence`, `Manual Push blocked evidence marker filename`, `Manual Push blocked evidence marker path`, `Manual Push blocked evidence marker present`, `Manual Push blocked evidence UTC`, `Manual Push blocked evidence UTC parseable`, `Manual Push blocked evidence selected branch`, `Manual Push blocked evidence selected branch selection kind`, `Manual Push blocked evidence selector mode`, `Manual Push blocked evidence selected version`, `Manual Push blocked evidence selected version slot kind`, `Manual Push blocked evidence selected version slot directory`, `Manual Push blocked evidence matches selected branch`, `Manual Push blocked evidence recorded prerequisites satisfied`, `Manual Push blocked evidence recorded local backup count`, `Manual Push blocked evidence recorded cloud backup count`, `Manual Push blocked evidence recorded latest local backup UTC`, `Manual Push blocked evidence recorded latest cloud backup UTC`, `Manual Push blocked evidence recorded pre-Push backup evidence satisfied`, `Manual Push blocked evidence reason`, `Manual Push blocked before upload evidence recorded`, `Important Android local save evidence count in bounded scan`, `Important Android local save evidence present`, `Backup storage permission available`, `Backup storage directory`, `Backup storage directory exists`, `Branch-switch manual Push prerequisites satisfied`, `Pre-Push local backup evidence count`, `Pre-Push cloud backup evidence count`, `Latest pre-Push local backup UTC`, `Latest pre-Push cloud backup UTC`, `Pre-Push local backup evidence after branch switch`, `Pre-Push cloud backup evidence after branch switch`, and `Branch-switch pre-Push backup evidence satisfied` with lowercase `true/false` values where applicable. `Manual Push completed after branch switch for selected version with backup evidence` must be true only when the latest manual Push evidence outcome is `completed`. Manual Push must fail before upload and write `last_manual_cloud_push_blocked.txt` when branch-switch marker evidence is incomplete or stale, Pull-after-switch evidence is missing, Android local save evidence is missing, backup storage permission is missing, local pre-Push backup coverage is incomplete for important Android saves, or cloud pre-Push backup coverage is incomplete for existing important Steam Cloud saves. Manual Push creates `last_manual_cloud_push.txt`, `local-pre-push`, and `cloud-pre-push` backup evidence covering the required important saves before upload once Pull evidence, local save evidence, and permission are available.
18. Validate diagnostics include `last_game_branch_switch.txt` after branch switching, with previous branch, selected branch, selected version, selected version slot kind, selected version slot directory, local-backup posture, manual Push backup-storage requirement, and non-public branch warning acknowledgement. Validate diagnostics include `last_manual_cloud_pull.txt` after Pull validation, with selected branch, selected version, selected version slot kind, selected version slot directory, selected branch note, and Pull-after-switch freshness for the selected version. Validate diagnostics include `last_manual_cloud_push.txt` after Push validation, with selected branch, selected version, selected version slot kind, selected version slot directory, selected branch note, local/cloud pre-Push backup counts, latest local/cloud backup UTC, completion flag, Push-after-switch freshness for the selected version, and recorded pre-Push backup evidence status. Validate diagnostics include `last_manual_cloud_push_blocked.txt` after fail-before-upload validation, with selected branch, selected version, selected version slot kind, selected version slot directory, prerequisite status, local/cloud pre-Push backup counts, latest local/cloud backup UTC, backup evidence status, blocked reason, and blocked-before-upload flag.

Current limitations:

- The UI is dropdown-first and can populate the dropdown from the latest Steam app-info branch availability marker after download/update app-info has been captured. It now exposes a non-mutating `REFRESH GAME VERSIONS` action that fetches Steam app-info branch metadata before download and refreshes the dropdown without downloading or deleting game files. Dropdown labels should remain concise for phone screens, while selector helper text should show the active install slot and selected-version availability details. Diagnostics should show `Steam branch selector mode: Steam branch dropdown`, selected branch preference key/source/selection kind, `Steam branch discovery supported: true`, Steam branch catalog source, Steam branch dropdown options, and Steam branch dropdown option metadata including source, metadata visibility, Windows manifest count, password flag, and build ID when available. Download/update logs and `last_steam_branch_availability.txt` should still surface account-visible Steam branch availability from app info.
- Steam login hardening uses an integrated native Android Steam credential panel as the Android credential target, with Godot login fields retained as the non-Android fallback. The launcher must not store, inject, or log Steam passwords. The native panel uses real Android username/password fields with Autofill hints and Steam web-domain metadata; the Steam Guard field requests a numeric keyboard. The previous native one-shot Autofill handoff dialog is no longer user-facing because it was a separate helper popup instead of the login surface. Password fields are cleared immediately after login request capture. Manual entry, keyboard/password-manager suggestion behavior, Steam Guard, failed login, and successful return to the launcher still need ARM64 device validation.
- Steam beta password entry is not implemented.
- If the selected non-public branch has no accessible manifests, download/update checks should fail with a branch-specific message naming missing/private/password-protected beta possibilities.
- Save-format compatibility between branches is unknown.
- ARM64 hardware remains the proof target for Steam login, depot download, branch switching, cloud saves, and game launch.

